import { DatePersianPipe } from './date-persian.pipe';

describe('DatePersianPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePersianPipe();
    expect(pipe).toBeTruthy();
  });
});
